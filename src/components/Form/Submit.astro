---
interface Props {
    label: string;
    class?: string;
};

const {
    label,
    class: className,
} = Astro.props;
---
<div class:list={['mb-3', className]}>

    <button
        type="submit"
        class="button btn btn-dark">
        {label}
    </button>

</div>
